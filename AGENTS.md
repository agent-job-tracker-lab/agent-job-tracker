# Codex 作業ルール

このファイルは、このリポジトリでCodexが作業するときの継続的な運用ルールを定める。
製品仕様や設計上の決定事項は各要求・設計資料へ記録し、本ファイルにはCodexの作業方法だけを記録する。

## 基本方針

- 既存資料にない製品仕様をCodexの判断だけで確定しない。
- 「既存資料から確定できる内容」「Developer案」「PMなどによる製品判断が必要な内容」を区別する。
- 実装時は要求定義、設計資料、対象Issue、PM回答を根拠とし、相互の矛盾を確認する。
- Issue、ブランチ、コミット、Pull Requestは、原則として1つの目的を追跡できる単位にする。
- GitHub ProjectへIssueを追加するときは、Status、Sprint、Priority、Estimate、Work typeを設定する。
- PMとDeveloperの役割を分け、Developerが実装・Pull Request作成、PMがレビュー・承認、ユーザーがマージする現在の運用を維持する。
- ブランチはマージ後も削除しない。

## Figmaの参照・更新方針

Figmaを実装Issueごとに機械的に開いたり更新したりしない。作業目的に応じ、次の優先順位で扱う。

1. API、DB、認証など画面表現に直接関係しないIssueでは、原則としてFigmaを参照しない。
2. UI実装Issueでは、リポジトリ内の要求・設計資料と `docs/design/wireframes/` のレビュー用PNGを先に参照する。
3. 上記の資料だけでは判断できない、資料間に矛盾がある、またはFigmaとの差異が疑われる場合に限り、編集可能なFigma原本を参照する。
4. Figma原本の更新は、ワイヤーフレームやデザイン自体の変更を目的とするIssue、または明示的に更新を依頼された場合だけ行う。
5. 通常の実装Issueで、実装結果をFigmaへ逆反映する作業は行わない。

FigmaはDESIGN-03の編集可能な原本、リポジトリ内PNGはGitHubレビューと実装参照用のスナップショットとして扱う。
両者に差異がある場合は勝手に一方へ合わせず、差異と影響を報告して判断を確認する。

## UI実装の確認方法

- 実装画面の位置ずれ、見切れ、レスポンシブ表示、操作可否は、実際のアプリをブラウザで表示して確認する。
- 必要に応じてE2Eテストやスクリーンショットを使い、主要なデスクトップ幅とスマートフォン幅を確認する。
- Figmaを開いたこと自体を動作確認の代わりにしない。
- UIが設計資料と一致しているかの確認と、実装がブラウザ上で正常に表示・操作できるかの確認を分けて行う。

## 記録の使い分け

- 製品要求・業務ルール: `docs/requirements/`
- Sprint固有の設計判断: `docs/design/`
- 計画、Issue構成、バックログ: `docs/planning/`
- Codexを含む開発エージェントの作業ルール: 本 `AGENTS.md`

作業中に新しい製品判断が承認された場合は、本ファイルではなく該当する要求・設計資料へ反映する。
Codexの進め方そのものを変更した場合は、本ファイルを更新する。
